const DATE_REGEX = /^\d{4}-\d{2}-\d{2}$/;
const TIMESTAMP = "timestamp";
const DEPOSIT = "DEPOSIT";
const SUPPORTED_TOKENS = ["ETH", "XRP", "BTC"];

module.exports = {
  DATE_REGEX,
  TIMESTAMP,
  DEPOSIT,
  SUPPORTED_TOKENS,
};
